spring.application.name=gateway-service
server.port=${US_PORT:9090}
server.address=${GATEWAY_SERVICE_HOST:localhost}
spring.cloud.config.uri=${SM_CLOUD_CONFIG_URL:http://localhost:9010}

#LOGSTASH CONFIG
sm.logstash.tcp.host=${SM_LOGSTASH_TCP_HOST:127.0.0.1}
sm.logstash.tcp.port=${SM_LOGSTASH_TCP_PORT:9700}
